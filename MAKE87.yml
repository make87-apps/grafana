version: 1
build:
  custom:
    dockerfile: Dockerfile
    platforms: [linux/amd64, linux/arm64]

interfaces:
  - name: grafana-http
    protocol: http
    default_port:
      name: grafana
      protocol: HTTP
      target_port: 3000
    clients:
      - name: loki
        spec:
          string: loki